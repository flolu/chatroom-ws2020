export enum ApiRoutes {
  Admin = 'admin',
  User = 'user',
}

export enum AdminApiRoutes {
  SignIn = 'signin',
  Authenticate = 'auth',
}

export enum UserApiRoutes {
  SignIn = 'signin',
  Refresh = 'refresh',
}
